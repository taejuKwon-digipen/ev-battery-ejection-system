# 필요 센서 및 부품 상세

`HARDWARE.md`의 BOM을 센서별로 더 상세히 정리한 문서입니다. 구매 전 각 센서의 스펙 확인 포인트와 선택 기준을 담았습니다.

## 1. 온도 센서 — NTC 서미스터 (필수, BMS용)

- **역할**: 배터리 표면 온도 실시간 측정 → 과열/열폭주 전조(온도 급상승) 감지
- **추천 스펙**: B값 3950, 25℃ 기준 저항 10kΩ (가장 흔한 규격, 코드 기본값과 일치)
- **선택 포인트**:
  - 방수/방습 캡슐형(스테인리스 프로브) 타입이 배터리 표면 부착에 유리
  - 응답속도가 빠른 소형 비드(bead) 타입도 가능하나 고정이 번거로움
- **대안**: DS18B20(디지털 온도센서, 1-Wire) — 더 정확하지만 코드 수정 필요 (현재 코드는 아날로그 NTC 기준)

## 2. 전류 센서 — ACS712 (필수, BMS용)

- **역할**: 배터리 라인의 과전류/이상 부하 감지
- **모델 선택 기준**: 예상되는 최대 전류보다 여유 있는 정격을 선택
  - 5A 모듈 (185mV/A) — 소형 모빌리티, 저전력 모터 조합에 적합
  - 20A 모듈 (100mV/A) — 중형 모터 여러 개 사용 시
  - 30A 모듈 (66mV/A) — 구동 전류가 큰 경우
- **주의**: 모델을 바꾸면 코드의 `ACS712_SENSITIVITY_V_PER_A` 값도 반드시 함께 변경 (`CAUTIONS.md` 참고)
- **대안**: 션트 저항 + INA219 (I2C 방식, 정밀도 높지만 배선/코드 복잡도 증가)

## 3. 전압 센서 — 분압 저항 회로 (필수, BMS용)

- **역할**: 과충전/과방전(전압 급강하 = 내부 단락 의심) 감지
- **구성**: 저항 2개로 분압 (예: 100kΩ + 10kΩ → 최대 약 55V까지 측정 가능, 배터리 정격 전압에 맞춰 비율 재계산)
- **대안**: 완제품 "전압 센서 모듈"(예: 0-25V 전압 감지 모듈)을 구매하면 저항 계산 없이 바로 아날로그 핀에 연결 가능
- **주의**: 분압 후 전압이 아두이노 ADC 최대 입력(5V)을 넘지 않도록 반드시 계산 후 사용

## 4. 장애물 회피 센서 (제안서 1단계, 선택 — 현재 코드에는 미포함)

기초 주행 로직용으로 제안서에 포함된 항목입니다. 현재 `BatteryEjectionSystem.ino`에는 구현되어 있지 않으며, 필요 시 추가 스케치/모듈로 확장합니다.

- **초음파 센서 (HC-SR04)**: 전방 장애물까지 거리 측정, 트리거/에코 2핀 방식으로 배선 간단, 아두이노 예제 라이브러리 풍부
- **IR 적외선 센서**: 근거리 장애물 또는 라인트레이싱용 보조 센서로 활용 가능, 초음파 대비 각도/반사면에 따라 오차 큼

## 5. 구동/기구 부품

- **서보모터 (래치 잠금/해제용)**:
  - SG90(소형, 저토크) — 가벼운 배터리 팩(수백g 이하) 및 마찰이 적은 래치 구조에 적합
  - MG996R 등 고토크 서보 — 배터리 팩이 무겁거나 래치가 스프링/마찰 저항을 가질 경우
  - **선택 기준**: 서보 토크(kgf·cm) × 모멘트암(cm) ≥ 배터리 팩 무게가 걸쇠에 가하는 최대 하중 모멘트
- **DC 모터 + 모터 드라이버(L298N 등)**: 차체 구동용, 채널 수는 2륜/4륜 독립 구동 여부에 따라 결정

## 6. 표시/입력 부품

- **LED (녹/황/적) 3개 + 저항(220~330Ω)**: 상태 표시
- **부저(수동/능동)**: 능동 부저는 `tone()` 없이도 소리가 나므로, 코드처럼 `tone()`으로 주파수 제어하려면 **수동 부저** 사용
- **택트 스위치**: 리셋 버튼용, 풀업 저항은 아두이노 내부 풀업(`INPUT_PULLUP`)으로 대체 가능해 외부 저항 불필요

## 7. 최소 구매 체크리스트 (BMS 데모 기준)

- [ ] NTC 서미스터 10kΩ + 분압저항 10kΩ
- [ ] ACS712 모듈 (전류 규격 확정 후 구매)
- [ ] 전압 분압용 저항 2종 또는 전압 센서 모듈
- [ ] 서보모터 1개 (래치 하중 고려해 SG90/MG996R 중 선택)
- [ ] LED 3색 + 저항, 수동 부저, 택트 스위치
- [ ] 아두이노 Uno(또는 호환보드), 브레드보드/점퍼선

장애물 회피(HC-SR04, IR)는 1단계 주행 로직 확장 시 추가로 구매하면 됩니다.
