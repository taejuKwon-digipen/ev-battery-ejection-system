# 주의사항 (아두이노 코드 사용 전 필독)

`arduino/BatteryEjectionSystem/BatteryEjectionSystem.ino`는 예시 캘리브레이션 값으로 작성되어 있습니다. 실제 부품/배터리에 맞춰 아래 값들을 반드시 실측 후 조정하세요.

## 1. 센서 캘리브레이션

| 항목 | 코드 위치 | 현재 예시값 | 확인/조정 방법 |
| --- | --- | --- | --- |
| NTC 서미스터 정격 저항/B계수 | `NTC_NOMINAL_RES`, `NTC_B_COEFFICIENT` | 10kΩ, B=3950 | 사용하는 서미스터 데이터시트 값으로 교체 |
| NTC 분압 저항 | `NTC_SERIES_RESISTOR` | 10kΩ | 실제 배선에 사용한 저항값과 일치해야 함 |
| ACS712 감도 | `ACS712_SENSITIVITY_V_PER_A` | 0.185 V/A (5A 모듈 기준) | 20A 모듈=0.100, 30A 모듈=0.066으로 변경 |
| ACS712 무전류 기준전압 | `ACS712_ZERO_CURRENT_V` | 2.5V | 실제 전원 전압(5V)의 절반이 아닐 수 있으므로 무부하 상태에서 직접 측정 후 대입 |
| 전압 분압비 | `VOLTAGE_DIVIDER_RATIO` | 11.0 (100k+10k 기준) | 실제 사용한 저항 조합으로 재계산: (R1+R2)/R2 |

값이 안 맞으면 정상 상태에서도 WARNING/STOPPED로 오진입하거나, 반대로 위험 상황을 감지하지 못할 수 있습니다.

## 2. 안전 임계값

`TEMP_WARNING_C`, `TEMP_CRITICAL_C`, `TEMP_RISE_RATE_C_S`, `CURRENT_WARNING_A`, `CURRENT_CRITICAL_A`, `VOLTAGE_MIN_V`, `VOLTAGE_MAX_V` 값은 실제 사용하는 배터리 셀의 스펙(정격 전압/전류, 제조사 권장 최대 온도)에 맞춰 재설정해야 합니다. 임의의 예시값을 그대로 실제 배터리에 적용하지 마세요.

## 3. 테스트 순서 (권장)

1. **배터리 없이** 서보 래치 동작(잠금 ↔ 해제)과 LED/부저 상태 표시부터 먼저 검증
2. 센서 3종(온도/전류/전압)을 개별적으로 시리얼 플로터로 확인해 필터링된 값이 안정적인지 확인
3. 저전력/저용량 테스트 배터리로 임계값 도달 시나리오를 강제 재현해 FSM 전이가 의도대로 되는지 확인
4. 실제 구동 배터리를 연결하는 것은 위 과정이 모두 검증된 이후에 진행

## 4. 기구/기계적 주의사항

* 래치 서보의 토크가 배터리 팩 무게와 주행 중 진동/충격을 버틸 수 있는지 확인 (평상시 실수로 풀리면 안 됨)
* 배터리 낙하 경로에 다른 부품(배선, 모터, 바퀴)이 걸리지 않는지 확인
* `EJECT_DELAY_MS`(현재 3초)는 데모용 값이며, 실제 대피가 필요한 시나리오에서는 상황에 맞게 조정

## 5. 코드 동작상 참고

* `PIN_MOTOR_EN`은 STOPPED 상태 진입 시 LOW로 내려가며, 이 핀에 실제 모터 드라이버 EN(Enable) 핀이 연결되어 있어야 정차가 반영됩니다.
* EJECTED 상태는 리셋 버튼(`PIN_RESET_BTN`)을 눌러야만 NORMAL로 복귀합니다 (반복 테스트용 설계이며, 실차 적용 시에는 물리적 재장착 절차와 연동 필요).
